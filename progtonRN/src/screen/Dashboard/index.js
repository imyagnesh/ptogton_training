import React from 'react';
import { SafeAreaView, Text } from 'react-native';

const index = () => {
  return (
    <SafeAreaView>
      <Text>Dashboard</Text>
    </SafeAreaView>
  );
};

export default index;
